# BusinessV2 Monorepo

This is a monorepo containing the BusinessV2 application.

## Structure

- `apps/web`: Frontend application (React + Vite)
- `apps/api`: Backend application placeholder

## Getting Started

To run the frontend:

```bash
npm install
npm run dev
```

This will start the web application in `apps/web`.
